import styled from 'styled-components';
import { border, layout, position, space, color } from 'styled-system';

export const Box = styled.div`
  ${space};
  ${layout};
  ${position};
  ${border};
  ${color};
`;
